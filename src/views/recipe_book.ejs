 <!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/css/form_styles.css">
    <%- include('partials/head'); %>
  </head>
  <body>
    <nav>
      <%- include('partials/nav'); %>
    </nav>
    <div class="bodyText">
      <h1>Recipe Book</h1>
      <p>Browse a list of all your saved recipes here.</p>
      <table>
          <% for(i=0; i<recipe_list.length; i++) { %>
              <tr>
                  <td><a href="/recipe_book/<%= recipe_list[i]._id %>"> <%= recipe_list[i].name %> </a></td>

                  <% if(recipe_list[i].author != "") { %>
                    <td>(<%= recipe_list[i].author %>)</td>
                  <% } %>

                  <td>
                    <!--<span class="edit_icon">
                      <a href="/edit_recipe/<%= recipe_list[i]._id %>">
                        <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                      </a>
                    </span>-->
                    <form method="POST" action="recipe_book/<%= recipe_list[i]._id %>?_method=delete" enctype="application/x-www-form-urlencoded">
                      <input type="submit" value="Delete" class="button delete_button" />
                    </form>
                  </td>
                  
              </tr>
          <% } %>
      </table>
    </div>
  </body>
</html>